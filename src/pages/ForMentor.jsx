import { Page } from "components/Page/Page";

export const ForMentor = () => {
  const longHeader = {
    fontSize: 28,
  };
  return (
    <Page linkBack="/" pageName="Для наставника" sizeName={longHeader}>
      ліст ітеми
    </Page>
  );
};
